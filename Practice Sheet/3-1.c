#include <stdio.h>
#include <math.h>
int main ()
{
    int num,res;
    printf("Enter any number =");
    scanf("%d",&num);
    res = abs(num);
    printf("The absolute value is %d",res);
    return 0;

}

